import React from "react";
import MenuList from "./List";

const Menu = () => {
  return (
    <div className="flex justify-center items-center w-full  backdrop-blur-4 rounded-t-[10px] shadow-md bg-white">
      <MenuList />
    </div>
  );
};

export default Menu;
